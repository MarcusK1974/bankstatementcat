Examples are intentionally small and safe to run.

- parse_transaction_minimal.py: parses a retrieve-transactions JSON and prints a redacted preview.
- build_manifest_minimal.py: builds a deterministic manifest for a folder.
